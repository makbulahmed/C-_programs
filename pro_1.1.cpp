#include <iostream>
using namespace std;

int main() {

    int first_no, second_no, sum;

    cout << "Enter two integers :: ";
    cin >> first_no >> second_no;
    sum = first_no + second_no;

    cout << first_no << " + " << second_no << " = " << sum;

    return 0;
}